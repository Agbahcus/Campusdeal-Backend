# Core Django (LTS version - supported until April 2026)
Django==4.2.17

# Django REST Framework (stable, widely used)
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# Database
psycopg2-binary==2.9.10
dj-database-url==2.1.0

# Image handling (security updates important here)
Pillow==11.0.0

# Environment variables (correct package name!)
python-decouple==3.8

# CORS headers
django-cors-headers==4.6.0

# HTTP requests (for Paystack, SMS APIs)
requests==2.32.3

# Phone number validation
django-phonenumber-field==8.0.0
phonenumbers==8.13.49

# Timezone support (built into Django 4.2, but keeping for compatibility)
pytz==2024.2

# Additional recommended packages
# Gunicorn for production deployment
gunicorn==23.0.0

# Whitenoise for serving static files in production
whitenoise==6.8.2

# Django filter for advanced querying
django-filter==24.3

# Python dotenv for .env file support
python-dotenv==1.0.1

# Rate limiting for security
django-ratelimit==4.1.0

# Realtime websockets
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2

# Cloudinary for media storage (production)
cloudinary==1.41.0
django-cloudinary-storage==0.3.0

# Sentry for error tracking (production)
sentry-sdk==1.40.0
